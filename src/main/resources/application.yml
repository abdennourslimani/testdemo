

server:
  port: 9000


# spring conf

spring :
  #App conf
    application:
        name: api

# mysql Conf
    datasource:
      url : jdbc:mysql://localhost:3306/noeldb
      username : root
      password : root

  #jpa conf
    jpa:
      hibernate:
        ddl-auto: update
